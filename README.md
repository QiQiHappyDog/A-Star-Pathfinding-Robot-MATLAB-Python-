# A-Star-Pathfinding-Robot-MATLAB-Python-
This code creates a 10x10 grid with obstacles (red dots). The robot starts at one point and needs to reach a goal point. The code uses the A Algorithm* to calculate the best path by balancing the distance already moved (g) and the estimated distance left to the goal (h).
